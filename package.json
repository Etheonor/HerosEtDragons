{
  "name": "rollwith-hd",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter api dev",
    "dev:web": "pnpm --filter web dev",
    "dev:all": "pnpm --filter web dev & pnpm --filter api dev",
    "build": "pnpm --filter shared build && pnpm --filter web build && pnpm --filter api build",
    "check": "pnpm lint && pnpm format:check && pnpm typecheck && pnpm test",
    "lint": "oxlint",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check .",
    "typecheck": "pnpm --filter shared typecheck && pnpm --filter ingest typecheck && pnpm --filter web check && pnpm --filter api typecheck",
    "test": "vitest run",
    "portraits": "node tools/portraits/build.mjs",
    "test:watch": "vitest",
    "db:generate": "pnpm --filter api db:generate",
    "db:migrate": "pnpm --filter api db:migrate",
    "db:migrate:remote": "pnpm --filter api db:migrate:remote",
    "deploy": "pnpm build && wrangler deploy",
    "ingest": "pnpm --filter ingest ingest"
  },
  "devDependencies": {
    "oxfmt": "0.58.0",
    "oxlint": "^1.14.0",
    "sharp": "^0.35.4",
    "vitest": "^3.2.4",
    "wrangler": "4.127.1"
  },
  "packageManager": "pnpm@10.20.0"
}
